apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
namespace: platform
metadata:
  name: vaultdbroles.platform.qvantel.com
spec:
  group: platform.qvantel.com
  scope: Namespaced
  names:
    # plural name to be used in the URL: /apis/<group>/<version>/<plural>
    plural: vaultdbroles
    # singular name to be used as an alias on the CLI and for display
    singular: vaultdbrole
    # kind is normally the CamelCased singular type. Your resource manifests use this.
    kind: VaultDbRole
    # shortNames allow shorter string to match your resource on the CLI
    shortNames:
    - vdbr
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: Vault Database Role definition
          required:
            - spec
          properties:
            spec:
              type: object
              properties:
                role-name:
                  type: string
                  description: The name for this role.
                db-name:
                  type: string
                  description: The name of the database connection to use for this role.
                default-ttl:
                  type: string
                  description: Specifies the TTL for the leases associated with this role. Accepts time suffixed strings (1h) or an integer number of seconds. Defaults to system/engine default TTL time.
                max-ttl:
                  type: string
                  description: pecifies the maximum TTL for the leases associated with this role. Accepts time suffixed strings (1h) or an integer number of seconds. Defaults to sys/mounts's default TTL time; this value is allowed to be less than the mount max TTL (or, if not set, the system max TTL), but it is not allowed to be longer.
                creation-statement:
                  type: string
                  description: Specifies the database statements executed to create and configure a user. See the plugin's API page for more information on support and formatting for this parameter.
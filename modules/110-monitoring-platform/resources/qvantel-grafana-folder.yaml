apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: grafanafolders.platform.qvantel.com
spec:
  group: platform.qvantel.com
  scope: Namespaced
  names:
    # plural name to be used in the URL: /apis/<group>/<version>/<plural>
    plural: grafanafolders
    # singular name to be used as an alias on the CLI and for display
    singular: grafanafolder
    # kind is normally the CamelCased singular type. Your resource manifests use this.
    kind: GrafanaFolder
    # shortNames allow shorter string to match your resource on the CLI
    shortNames:
    - gf
  versions:
    - name: v1
      additionalPrinterColumns:
        - description: The state of the the resource
          jsonPath: .status.conditions[?(@.type=="Ready")].status
          name: Ready
          type: string
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: Grafana Dashboard Folder definition
          required:
            - spec
          properties:
            spec:
              type: object
              properties:
                path:
                  type: string
                  description: Name of the keycloak role to be admins in the team.
                permissions:
                  type: array
                  description: Name of the keycloak role to be members in the team.
                  items:
                    type: object
                    properties:
                      team:
                        type: string
                      level:
                        type: string
                        enum:
                          - admin
                          - edit
                          - view
                          
            status:
              description: The status of the deployment.
              type: object
              properties:
                conditions:
                  description: List of status conditions.
                  items:
                    properties:
                      forceSync:
                        type: integer
                      lastTransitionTime:
                        description: Last time the condition of a type changed from
                          one status to another. The required format is 'yyyy-MM-ddTHH:mm:ssZ',
                          in the UTC time zone.
                        type: string
                      message:
                        description: Human-readable message indicating details about
                          the condition's last transition.
                        type: string
                      reason:
                        description: The reason for the condition's last transition
                          (a single word in CamelCase).
                        type: string
                      status:
                        description: The status of the condition, either True, False
                          or Unknown.
                        type: string
                      type:
                        description: The unique identifier of a condition, used to distinguish
                          between other conditions in the resource.
                        type: string
                    type: object
                  type: array
                observedGeneration:
                  description: The generation of the CRD that was last reconciled by
                    the operator.
                  type: integer
        # subresources for the custom resource
      subresources:
        # enables the status subresource                
        status: {}
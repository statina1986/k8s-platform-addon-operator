apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: mariadb-platform
    app.kubernetes.io/name: mariadb
  name: mariadb
  namespace: platform
spec:
  ports:
  - name: mysql
    port: 3306
    protocol: TCP
    targetPort: mysql
  selector:
    app.kubernetes.io/component: primary
    app.kubernetes.io/instance: mariadb-platform
    app.kubernetes.io/name: mariadb
  sessionAffinity: None
  type: ClusterIP
{{- if eq .Values.global.clusterwideResources "true" }}
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  annotations:    
    cert-manager.io/inject-ca-from: "{{ .Release.Namespace }}/{{ .Values.global.helmReleaseNamePrefix }}vault-platform-vault-secrets-webhook-ca"
  name: vault-platform-vault-secrets-webhook-for-{{.Release.Namespace}}
webhooks:
- admissionReviewVersions:
  - v1beta1
  clientConfig:
    caBundle: ""
    service:
      name: {{ .Values.global.helmReleaseNamePrefix }}vault-platform-vault-secrets-webhook
      namespace: {{.Release.Namespace}}
      path: /pods
      port: 443
  failurePolicy: Fail
  matchPolicy: Equivalent
  name: pods.vault-secrets-webhook.admission.banzaicloud.com
  namespaceSelector:
    matchExpressions:
    - key: name
      operator: NotIn
      values:
      - kube-system
    - key: kubernetes.io/metadata.name
      operator: NotIn
      values:
      - kube-system
  objectSelector:
    matchExpressions:
    - key: security.banzaicloud.io/mutate
      operator: In
      values:
      - apply
  reinvocationPolicy: Never
  rules:
  - apiGroups:
    - '*'
    apiVersions:
    - '*'
    operations:
    - CREATE
    resources:
    - pods
    scope: '*'
  sideEffects: NoneOnDryRun
  timeoutSeconds: 10
{{- end }} 
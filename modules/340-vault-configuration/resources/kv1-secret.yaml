apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kv1secrets.platform-vault.qvantel.com
spec:
  group: platform-vault.qvantel.com
  scope: Namespaced
  names:
    # plural name to be used in the URL: /apis/<group>/<version>/<plural>
    plural: kv1secrets
    # singular name to be used as an alias on the CLI and for display
    singular: kv1secret
    # kind is normally the CamelCased singular type. Your resource manifests use this.
    kind: KV1Secret
    # shortNames allow shorter string to match your resource on the CLI
    shortNames:
    - kv1s
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          description: KV Version 1 Secret
          required:
            - spec
          properties:
            spec:
              type: object
              properties:
                path:
                  type: string
                  description: Path for the secret.
                secret:
                  type: object
                  description: Key-value pairs of the secret.
                  additionalProperties: true
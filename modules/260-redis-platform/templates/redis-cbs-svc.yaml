---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis-mastercbs-leader
    redis_setup_type: cluster
    role: leader
  name: redis-cluster-redis-mastercbs
spec:
  ports:
  - name: redis-client
    port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis-mastercbs-leader
    redis_setup_type: cluster
    role: leader
# type: LoadBalancer
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis-controlcbs-leader
    redis_setup_type: cluster
    role: leader
  name: redis-cluster-redis-controlcbs
spec:
  ports:
  - name: redis-client
    port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: redis-controlcbs-leader
    redis_setup_type: cluster
    role: leader
# type: LoadBalancer
  type: ClusterIP
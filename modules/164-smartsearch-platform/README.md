
# Smartsearch standalone module

This module requires the eck-operator in order to deploy the smartsearch nodes. Currently the eck-operator is deployed using the 160-elasticsearch-platform module, but because we are going to be moving away from ELK-stack logging this might chance in the future.

Depends on modules:
- 160-elasticsearch-platform